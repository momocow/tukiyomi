extends = 'build/local-build.toml'

artifactName = "${productName}-v${version}-build.${env.TRAVIS_BUILD_NUMBER}.${ext}"

publish = [
  "github"
]

[nsis]
artifactName = "${productName}-setup-v${version}-build.${env.TRAVIS_BUILD_NUMBER}.${ext}"
oneClick = false
allowToChangeInstallationDirectory = true
multiLanguageInstaller = true
